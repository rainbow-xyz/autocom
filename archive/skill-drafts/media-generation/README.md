# Archived media-generation draft

这里归档早期 `media-generation` skill 草稿。当前正式能力已经拆分为：

```text
skills/image-generation
skills/video-generation
```

归档目录下不能保留 `SKILL.md`，否则 Paperclip 这类递归扫描工具会把历史草稿误识别为可安装 skill。

需要回查旧实现时，只查看本目录中的 provider 脚本、示例和配置；不要把这个目录作为 agent skill 安装。
